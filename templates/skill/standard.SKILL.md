---
name: <kebab-case-name, must match the directory name>
description: <Third person, naming triggers. For a standard, the triggers are usually artifact types: "when writing or reviewing X, Y, or Z".>
# requires: [other-skill]   # hard dependency; missing one is an error
# see_also: [peer-skill]    # cross-reference; still works alone
# owns: <the exact territory this standard is authoritative for>
---

# <Title>

<What good looks like, in one or two sentences. State the goal the standard serves,
because that is what an author falls back on when a rule does not fit.>

<Then the two or three rules that outrank everything below, including an escape
hatch. A standard with no escape hatch gets abandoned wholesale the first time it
produces a bad result.>

## Scope

<Say exactly what this covers and what it does not, and name the sibling artifact
that owns the neighbouring territory. This is the section that prevents two standards
from contradicting each other.>

## <Criterion group>

- <Criteria, not steps. Each one should be checkable against a finished artifact.>
- <Put the non-obvious ones first. The obvious ones are already in the base prompt.>

Source: <url or citation>, fetched <YYYY-MM-DD>.

## Worked example

Before:

> <A realistic bad version. Not a strawman.>

After:

> <The fixed version.>

<Then name which criterion fixed what. This is the highest-value part of a standard:
it shows the criteria interacting, which a list of rules cannot.>

## Review checklist

<Numbered questions to run against a finished artifact. This turns the standard from
something that guides authoring into something that can audit existing work.>

1. <Question that has a yes/no answer.>
2. <...>
