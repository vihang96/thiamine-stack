# A project that must produce findings. If `ruff check` here is quiet, the lint layer is
# not loaded, which looks exactly like clean code.
[project]
name = "thiamine-lint-fixture"
version = "0.1.0"
requires-python = ">=3.11"
